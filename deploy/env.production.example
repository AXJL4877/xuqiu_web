# 复制为项目根目录 .env.production 后修改
# cp deploy/env.production.example .env.production

# PostgreSQL（docker-compose 内网，勿改主机名 postgres）
POSTGRES_USER=xuqiu
POSTGRES_PASSWORD=请改为强密码
POSTGRES_DB=xuqiu

# 对外 HTTP 端口（阿里云安全组需放行此端口）
HTTP_PORT=80
